---
import Funnel from '../layouts/Funnel.astro';
import Vsl from '../components/Vsl.astro';
---
<Funnel title="UPSELL 01 PAYT – A Sociedade Trader" poster="/assets/poster-upsell-01.webp">
  <!-- gate: esconde os botões até 4:14 após o play. Roda antes da pintura (sem flash);
       sem JS, nada é escondido (fallback). -->
  <script is:inline>try{if(!localStorage.getItem('al_up01_revealed')&&!/[?&]preview/.test(location.search))document.documentElement.classList.add('gated')}catch(e){document.documentElement.classList.add('gated')}</script>
  <div class="scarcity">⚠️ IMPORTANTE: ESSA PÁGINA SAIRÁ DO AR EM <span id="cd">15:00</span></div>
  <main class="funnel">
    <div class="funnel-wrap">
      <h1 class="funnel-title">Sua compra ainda<br />não está completa…</h1>
      <ul class="steps-card">
        <li><span class="ic">✅</span> Passo 1: Sociedade do Trader</li>
        <li class="is-now"><span class="ic">⚙️</span> Passo 2: Configuração blindada</li>
        <li><span class="ic">⏳</span> Passo 3: Compra concluída!</li>
      </ul>
      <Vsl playerId="vid-6a3d2078ab74e8fd0f267310" poster="/assets/poster-upsell-01.webp" />
      <div class="gate" id="up01-cta">
        <button class="accept-btn kirvano-payment-trigger" type="button">QUERO GARANTIR MINHA CONFIGURAÇÃO BLINDADA.</button>
        <button class="decline kirvano-refuse-trigger" type="button">NÃO QUERO ESSA OFERTA</button>
      </div>
    </div>
  </main>
  <!-- Kirvano upsell (funil montado no Kirvano) -->
  <script is:inline>
    var offer = "83f13966-2013-4f39-9f31-ff190ec4c5ab";
    var nextPageURL = "https://lp05.irmandadedodolar.com.br/priority-pass-2/";
    var refusePageURL = "https://lp05.irmandadedodolar.com.br/downsell-payt/";
  </script>
  <script is:inline src="https://snippets.kirvano.com/upsell.min.js"></script>
  <!-- reveal timer: 4:14 após o play. Robusto a 2º plano (Date.now) + ?preview p/ QA. -->
  <script is:inline>
    (function () {
      var DELAY = (4 * 60 + 10) * 1000; // 4:10 = 250s
      var KEY = 'al_up01_revealed', started = 0, iv = null;
      function reveal() {
        document.documentElement.classList.remove('gated');
        try { localStorage.setItem(KEY, '1'); } catch (e) {}
        if (iv) { clearInterval(iv); iv = null; }
      }
      function check() { if (started && Date.now() - started >= DELAY) reveal(); }
      try { if (/[?&]preview/.test(location.search)) { document.documentElement.classList.remove('gated'); return; } } catch (e) {}
      try { if (localStorage.getItem(KEY)) { reveal(); return; } } catch (e) {}
      window.__startVslTimer = function () {
        if (started) return;
        started = Date.now();
        setTimeout(reveal, DELAY);
        iv = setInterval(check, 1000);
        document.addEventListener('visibilitychange', check);
        window.addEventListener('focus', check);
      };
    })();
  </script>
</Funnel>
